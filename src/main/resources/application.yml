spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/olive?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: kim111

logging:
  level:
    root: info

server:
  port: 8080
